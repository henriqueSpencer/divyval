# Cloudflare Pages — projeto DIVYVAL.
# Estáticos servidos da raiz deste diretório; as Functions vivem em ./functions.
# Rodar local:  npx wrangler pages dev dashboard   (a partir da raiz do repo)
name = "divyval"
pages_build_output_dir = "."
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]
