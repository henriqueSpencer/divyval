# Dependências do backend DIVYVAL em produção (Render).
# yfinance puxa pandas/numpy/requests/curl_cffi automaticamente.
fastapi==0.139.0
uvicorn[standard]==0.49.0
yfinance==1.5.1
# Persistência: PostgreSQL (Supabase) via psycopg 3. Conexão vem de DATABASE_URL.
psycopg[binary]>=3.2
